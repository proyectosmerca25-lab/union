# UNIÓN Local PostgreSQL Development Environment Template
# Copy to .env for local overrides (never committed to git)

POSTGRES_DB=union
POSTGRES_USER=union_app
POSTGRES_PASSWORD=union_dev_password
POSTGRES_PORT=5432
