# UNIÓN Local PostgreSQL Development Environment Template
# Copy this file to .env and replace POSTGRES_PASSWORD with an explicit local secret.
# DO NOT commit .env or real operational secrets to version control.

POSTGRES_DB=union
POSTGRES_USER=union_app
POSTGRES_PASSWORD=<replace_with_explicit_local_password>
POSTGRES_PORT=5432
