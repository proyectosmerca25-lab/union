{
  "name": "union",
  "version": "0.1.0",
  "private": true,
  "description": "UNIÓN — Private Multi-Project Governance & Coordination System Monorepo",
  "author": "Omar",
  "type": "module",
  "engines": {
    "node": ">=24.0.0",
    "npm": ">=10.0.0"
  },
  "workspaces": [
    "shared",
    "union-core",
    "union-web"
  ],
  "scripts": {
    "build": "tsc -b && npm run build --workspace=@union/web",
    "typecheck": "tsc -b",
    "test": "npm run build && npm run test --workspaces",
    "clean": "tsc -b --clean",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "db:status": "docker compose ps"
  },
  "devDependencies": {
    "@types/node": "^22.13.4",
    "typescript": "^5.7.3"
  }
}
